@top Program { expression* }

@skip { space }

expression {
  Identifier |
  Application { "(" expression* ")" }
}

@tokens {
  Identifier { $[a-zA-Z_\-0-9]+ }
  space { $[ \t\n\r]+ }
  "(" ")"
}

@detectDelim
